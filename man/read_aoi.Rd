% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aoi.R
\name{read_aoi}
\alias{read_aoi}
\title{Read and validate a polygon study area}
\usage{
read_aoi(x, layer = NULL)
}
\arguments{
\item{x}{An `sf` or `sfc` polygon, a path to a vector file, or a Shiny
upload object containing `name` and `datapath`.}

\item{layer}{Layer name for a multi-layer GeoPackage. Required when there
is more than one layer.}
}
\value{
An `sf` polygon object in EPSG:4326. Empty geometries are removed.
}
\description{
Read and validate a polygon study area
}
\details{
Zipped Shapefiles must contain one Shapefile with a declared CRS.
  Archives with unsafe paths or more than 200 MB uncompressed are rejected.
  No coordinate reference system is guessed.
}
\examples{
ring <- matrix(c(0,0, 1,0, 1,1, 0,1, 0,0), ncol = 2, byrow = TRUE)
aoi <- read_aoi(sf::st_sfc(sf::st_polygon(list(ring)), crs = 4326))
}
