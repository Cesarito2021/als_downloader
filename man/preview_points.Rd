% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preview.R
\name{preview_points}
\alias{preview_points}
\title{Make a bounded point-cloud preview}
\usage{
preview_points(points, max_points = 100000L)
}
\arguments{
\item{points}{A data frame or matrix with numeric `X`, `Y`, `Z` columns.}

\item{max_points}{Maximum displayed points, between one and one million.}
}
\value{
A data frame of sampled coordinates with an `origin` attribute.
  Coordinates are translated to a local origin for display precision.
  An optional numeric `Classification` column is retained as an aligned
  `classification` attribute; missing or invalid codes become `NA`.
}
\description{
Make a bounded point-cloud preview
}
\details{
Sampling is deterministic and affects only the returned preview.
  The source cloud is not modified. Elevation is not normalized canopy height.
}
\examples{
preview_points(data.frame(X = 1:10, Y = 1:10, Z = 11:20), 5)
}
